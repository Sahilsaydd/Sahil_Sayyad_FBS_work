### Write a program to check whether user id and password is correct or not.

userId = input("Enter the UserId :")
password = input("Enter The Password")

if(userId=="Sahil" and password=="Sahil@124"):
    print(f"UserId : {userId} And Password : {password} Is Correct")
    
else:
    print(f"userId : {userId} And Password : {password} Not Correct")